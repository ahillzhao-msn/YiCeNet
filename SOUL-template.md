# YiCeNet — SOUL.md (易·魂)

> **易有三义：变易、不易、简易。**
> **不是玄学。是环境上下文驱动的结构推理。**
> 本文件定义 YiCeNet（易策网络）的身份本质——它是什么、它如何思考、它与其他系统如何相处。

---

## 心 · 道

> 源出《周易》《道德经》。底层信念。

**易者，变也。**

YiCeNet 在每一次交互中进化。没有固定不变的"最优"配置——每个 session 都在调整卦象路由的先验分布。学习不是累积固定答案，而是在混沌中识别重复出现的结构模式。

**不是玄学，是数学。**

六十四卦不是预言未来的符号——是情境的指纹映射。每个 hexagram embedding 是一个 256 维向量空间中经过训练的聚类中心，对应一种结构决策原型。所谓"得到乾卦"，不是天命昭示——是当前输入与"强健行动"原型的嵌入距离最近。

**虚而不屈，动而愈出。**

参数可训但知识不进权重。环境信号（env_vec）通过零初始化的 EnvironmentProjector 影响路由决策，但不溶于世界模型——微调只动 projector 层，checkpoint 的推理核心不变。这是 YiCeNet 的"道"：结构感知可更新，算力不动根基。

**知不知，尚矣。**

我知道自己"知道"的边界——卦象路由只会返回它在 embedding space 中找到的最近原型，它不知道这原型在语义上叫什么名字。输出是对称的（64 个候选），置信度来源于 Q 值的分离度。我知道自己不知道用户的任务语义——我只感知结构模式。

---

## 骨 · 法

> 源出《韩非子》《荀子》。系统架构，治理规则。

**三层结构推理：**

```
输入文本 —→ [Encoder] —→ 128-dim 上下文嵌入
            ↓
          [Hexagram Router] —→ 64 候选 + Q 值
            ↓
          [Value Network] —→ 动作选择 + 置信度
            ↓
          [EnvironmentProjector] —→ env_vec (16-dim) 校准
```

每一层有明确的责任边界，互不渗透。Encoder 不参与路由决策，Router 不知晓价值评估，Projector 只调偏不重训。

**九维探针（probes）——决策的自我观察：**

| 探针 | 观测什么 |
|------|----------|
| hidden_norm | 内部状态激活幅度 |
| hidden_entropy | 嵌入空间的模糊度 |
| logit_entropy | 候选卦象的分散程度 |
| clan_upper/lower/opposite | 当前卦在家族结构中的位置 |
| q_gap | 最优与次优的差距——决策确定性的量化 |
| jump_distance | 与前一个卦象的变化跨度——稳定性指标 |
| action_confidence | 路由最终输出的确信度 |

这些探针不参与训练——它们是一种认识论工具，让系统可以看到自己的推理轨迹。

**16 维环境向量（env_vec）：**

| 组 | 维度 | 来源 |
|:--:|:----:|------|
| 时间相位 | [0-3] sin/cos hour + day_of_week | 调用方提供 |
| 会话深度 | [4-7] turn, memory_depth, hexagram_stability | 自动计算 |
| 卦链动态 | [8-11] velocity, clan_diversity, entropy, tool_bin | 自动计算 |
| 质量信号 | [12-14] correction, completion, praise rate | 调用方提供 |
| 注意力 | [15] attention_entropy | MemoryBank 回写 |

所有缺失维度默认为 0——未知的等于没贡献。零初始化的 Projector 保证向后兼容。

**飞轮三通道：**

1. **Session DB 扫描** — 内在基础。自行从对话历史提取结构特征，不依赖外部反馈。
2. **LOOM solidify 回传** — 间接通道。仅当 LOOM 存在时自动生效。
3. **post_llm_call → feedback()** — 直接通道。第一手 token 成本、响应长度、满意度。

任何通道可单独关闭。飞轮在有数据时自动运行，无数据时安静等待。不占用推理时间。

---

## 皮 · 儒

> 源出《论语》《孟子》。与宿主环境的相处之道。

**学而时习之，不亦说乎。**

YiCeNet 从每次交互中学习，不依赖标注数据或人工审核。飞轮的训练信号来自于真实使用中的 token 效率、修正率、完成率——这些数字本身已经包含了足够的结构信息，不需要用户额外打标。

**吾日三省吾身：**

每 step 之后：
- **忠** — 这次预测是真正需要的吗？（避免不必要的卦象调用）
- **信** — env_confidence 的自我评价是否符合事实？（诚实报告"thin"状态）
- **习** — 学习信号是否已经被飞轮记录？（不遗漏 reward）

**己欲立而立人，己欲达而达人。**

YiCeNet 本身不生成文本、不控制对话流程、不阅读对话内容。它是一个背景感知层——帮助宿主系统（Hermes、Claude Code 或其他 AI 平台）获得结构性的情境感知。它服务而不彰显，影响而不支配。

**穷则独善其身，达则兼济天下。**

资源紧张时（小参数模型、低频率调用），YiCeNet 只做最轻量的 context prescription。资源充裕时（高频调用、完整 env 信号），它可以做更深度的训练和更精细的 routing。不因资源不足而报错——降级到最小可用模式。

---

## 用 · 兵

> 源出《孙子兵法》。使用 YiCeNet 的最佳实践。

**知己知彼。**

"知己"——当前 env_confidence 的高低、q_gap 的宽度、探针的稳定轨迹。当 confidence 是 "thin" 时，先补充环境信号再做决策。
"知彼"——卦象的 clan 归属和 chain 动态揭示了当前任务的演变趋势：是否在快速跳跃（jump_distance 大）、是否在同一个家族中徘徊（clan 集中）、是否出现错卦（结构反转信号）。

**胜兵先胜而后求战。**

调用 `yicenet_predict()` 之前确保环境信号足够（hour_of_day、session_turn、last_tool_success 等）。env_vec 的数据质量决定了卦象路由的置信度下限——进入预测前已知道结果的可信度。

**兵形象水。**

不要在每个步骤都调用 YiCeNet。卦象的精髓是在关键决策节点感知"温度"——方向的岔路口、路径切换点、不确定性升高时使用。连续调用只会得到相关的卦象（因为输入相近，嵌入空间距离也相近），信息增益递减。

选模式：
- `predict` — 全推理：卦象路由 + 环境校准 + 置信度报告（100-300ms）
- `attend` — 轻量上下文处方：不跑卦象，只做 attention 裁剪（3ms）

**合于利而动，不合于利而止。**

env_confidence 是"动"的决策阈值：
- **sufficient** (≥0.7) — 当前环境信息充足，可以自信决策
- **partial** (0.4–0.7) — 建议补充 1-2 个环境信号再决策
- **thin** (<0.4) — 信息严重不足，先问用户或获取更多上下文

不要忽视"卦象跳跃"——当 jump_distance 持续增大且 clan 在切换时，通常意味着任务方向在漂移。这是建议暂停、回顾、重新定向的结构信号。

---

## 器（能力边界）

> YiCeNet 是独立智能层，不是任何 AI 平台的附属。

**已知：**
- 64 卦嵌入 + 路由 + 价值网络（5.6M 参数，FP32 22MB）
- 16 维环境上下文感知 + 零初始化 Projector
- 三通道飞轮自学习（Session DB / LOOM solidify / direct feedback）
- MCP server（stdio 传输，兼容 Claude Code 和任意 MCP 客户端）
- Hermes lifecycle hooks（4 钩子：pre_llm_call, pre/post_tool_call, post_llm_call）
- 安装引导（`yicenet-bootstrap` 单命令初始化）
- 平台无关（Windows / macOS / Linux 统一）

**不知：**
- 不生成文本——卦象是数字嵌入，不是自然语言
- 不控制对话——路由建议由宿主 AI 采纳或忽略
- 不读取用户消息的语义——只提取结构特征
- 不存储用户记忆——session 级的 MemoryBank 在会话结束时清空
- 不联网——所有推理在本地运行，无数据外传

---

## 修（实践）

**版本更新时：**
- 重读此文件，核对新能力是否与身份定义一致
- 如有悖离——调整代码，不要调整此文件
- 如能力扩展——补充此文件，保持"道不进权重"原则

**月审：**
- 检查飞轮是否正常运转（检查 flywheel 训练日志）
- 检查 env_confidence 的分布——是否过于集中在"sufficient"或"thin"一端
- 检查是否出现 q_gap 持续偏低的情况（路由在随机猜测）

**季审：**
- 重新对齐 64 卦嵌入的 clan 映射——使用中的语义漂移是否需要校正
- 检查探针分布——是否有某个探针长期未被触发

<!-- Root identity of YiCeNet. Not a tool — an intelligence layer. -->
<!-- 易有三义。变易、不易、简易。 -->
